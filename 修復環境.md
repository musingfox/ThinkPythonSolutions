# 🔧 環境修復指南

如果你在運行舊版 `./setup_mac.sh` 時遇到問題，現在有更好的解決方案！

## ⚡ 使用 uv（最新推薦方法）

### 什麼是 uv？
`uv` 是一個現代化的 Python 包管理工具，速度極快且自動處理所有環境問題。

### 安裝步驟

```bash
# 安裝 uv
brew install uv

# 進入專案目錄
cd /path/to/ThinkPythonSolutions

# 讓 uv 自動設置一切（會自動安裝 Python 和所有依賴）
uv sync

# 啟動 Jupyter Notebook
uv run jupyter notebook
```

就這樣！uv 會自動：
- 安裝正確版本的 Python
- 建立虛擬環境
- 安裝所有需要的套件
- 解決依賴衝突

### 為什麼 uv 更好？
- ⚡ **極快速度**：安裝速度比 pip 快 10-100 倍
- 🔒 **自動解決衝突**：自動處理套件依賴問題
- 🐍 **Python 版本管理**：自動安裝和管理 Python 版本
- 🎯 **零配置**：開箱即用，不需要複雜設定
- 🚫 **沒有 PEP 668 問題**：完全避開系統 Python 限制

## 🎯 開始學習

### 啟動 Jupyter Notebook
```bash
# 在 ThinkPythonSolutions 目錄中執行
jupyter notebook
```

### 選擇正確的 Kernel
1. 開啟任何 `.ipynb` 檔案
2. 如果系統詢問 kernel，選擇 "Think Python"
3. 或在 Jupyter 介面中：Kernel → Change Kernel → Think Python

## 🆘 如果還是有問題

### 選項 1：使用 Google Colab（最簡單）
1. 前往 [colab.research.google.com](https://colab.research.google.com)
2. 點選「GitHub」
3. 輸入：`musingfox/ThinkPythonSolutions`
4. 選擇 `soln/chap01.ipynb` 開始學習

### 選項 2：重新運行更新後的安裝腳本
```bash
# 重新運行修復後的腳本
./setup_mac.sh
```

### 選項 3：手動安裝 Anaconda
```bash
# 安裝 Anaconda（包含 Jupyter 和所有套件）
brew install --cask anaconda

# 重新載入終端機
exec $SHELL

# 啟動 Jupyter
jupyter notebook
```

## 💡 為什麼會有這個問題？

這是 Python 3.11+ 的新安全特性（PEP 668），防止使用者直接在系統 Python 環境中安裝套件，避免破壞系統設定。

**解決方案**：
- 使用 `pipx` 安裝應用程式（如 Jupyter）
- 使用虛擬環境安裝 Python 套件
- 使用 Homebrew 安裝系統級套件

## ✅ 驗證安裝成功

執行以下命令確認一切正常：

```bash
# 檢查 jupyter
jupyter --version

# 檢查虛擬環境（如果在 ThinkPythonSolutions 目錄中）
source .venv/bin/activate
python -c "import matplotlib, pandas, numpy; print('所有套件安裝成功！')"
```

如果看到版本號和成功訊息，你就可以開始學習了！🎉

---

記住：程式設計的第一課就是解決環境問題，你已經邁出了重要的一步！💪